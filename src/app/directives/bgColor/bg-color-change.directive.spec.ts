import { BgColorChangeDirective } from './bg-color-change.directive';

describe('BgColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BgColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
